# OpenSignals Protocol MCP Server Requirements

# MCP SDK for Python
mcp>=1.0.0

# Standard library enhancements
python-dotenv>=1.0.0

# Optional: JSON schema validation
jsonschema>=4.0.0

# Optional: HTTP client for remote manifest fetching
httpx>=0.27.0

# Optional: Async support
anyio>=4.0.0
